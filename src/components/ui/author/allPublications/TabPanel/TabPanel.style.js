import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
	panel: {
		display: 'flex',
		flexWrap: 'wrap',
		paddingLeft: '18px',
		paddingTop: '3px',
	},
});

export default useStyles;
